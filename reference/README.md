# reference/

Put the reference genome + intervals here (paths set in config/config.yaml).
Default target organism is malaria (Plasmodium). `.fai` and `.dict` are built
automatically. **No genome data is committed** — see .gitignore.
