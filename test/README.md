# test/

Small bundled smoke-test dataset for the LOCAL (no-scheduler) run — rung 1 of
the test ladder. Tiny FASTQs + a small reference so `snakemake --cores N`
completes fast end-to-end. Dataset added in the build phase.
