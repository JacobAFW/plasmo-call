# SLURM profile

Adapted from vvg-box's slurm snakemake-profile (cluster-generic executor:
slurm-submit.py / slurm-status.py / slurm-jobscript.sh). Site values
(partition, account, qos) are parameters. Wired up in the build phase.
